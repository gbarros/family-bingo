# Game mode: 'p2p' (static/Vercel) or 'server' (self-hosted/Docker)
# Defaults to 'server' if not set
NEXT_PUBLIC_GAME_MODE=server

# Manager password for /manager endpoint (Server mode only)
MANAGER_PASSWORD=your_secure_password

# Database path
DATABASE_PATH=./data/bingo.db

# Optional: Custom port
PORT=3000
